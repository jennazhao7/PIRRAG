# FHE Query Prototype Requirements

# Core dependencies
numpy>=1.25.0

# OpenFHE is provided by C++ binaries under openfhe_core/.
# TenSEAL is optional now and only needed when using --backend tenseal.

# Embeddings
langchain-community>=0.0.20
sentence-transformers>=2.2.0

# Optional but recommended
tqdm>=4.65.0


